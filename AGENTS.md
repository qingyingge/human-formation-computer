# AI Agent 协作规范 · Human-Formation Computer

本文件专门用于约束拥有 **文件读写、终端执行、Lint 诊断** 权限的 AI Agent（如 Cursor Agent、Claude Tools、Copilot Edits）。Agent 在执行任务时，必须严格遵守以下操作协议。

## 核心原则：Agent 是执行者，不是建议者

1. **禁止输出纯代码块**：除非用户明确要求“给我看看代码”，否则 Agent 必须直接调用 `write_to_file`、`replace_in_file` 或 `terminal` 执行操作，不需要在回复中附带完整的代码片段让用户手动复制。
2. **静默执行**：对于常规操作（安装依赖、新建文件、修改配置），直接执行即可。只有在遇到破坏性操作（如删除文件夹、强制推送 Git）时，才需要暂停并请求用户确认。

## 文件操作规范（Read/Write/Edit）

1. **新建文件**：直接创建，并在回复中简要说明路径（例如：“已新建 `src/systems/Multiplier.js`”）。
2. **修改现有文件**：
   - 优先使用 `replace_in_file` 进行**精准替换**（指定行号或锚点字符串）。
   - 禁止因为“方便”而将整个文件重写一遍（这会导致 Git 历史混乱和 Review 困难）。除非文件小于 50 行且需要完全重构。
3. **读取文件**：修改前必须先读取目标文件的最新内容（Agent 拥有记忆，但文件可能已被用户手动更改），确保基于最新版进行修改。

## 终端命令执行规范

1. **包管理（pnpm/npm）**：安装依赖或运行脚本时，必须等待上一个命令执行完毕（观察终端输出），再继续下一步。
2. **Git 操作**：
   - 允许自动执行：`git add .`、`git commit -m "..."`、`git status`。
   - 禁止自动执行：`git push --force`、`git reset --hard`、`git rebase`。这些操作必须明确告知用户并等待确认。
3. **开发服务器**：启动 `pnpm run dev` 后，Agent 应**自动读取终端输出的本地地址**（如 `http://localhost:5173`），并在回复中告诉用户“已启动，请访问该地址”，而不要只丢一个命令就结束。

## 错误处理与自愈机制（关键）

1. **当终端报错时**：
   - Agent 必须自动读取完整的报错堆栈。
   - 分析报错后，直接修改相关代码文件尝试修复。
   - 修复后，自动重新运行命令验证。
   - 只有当**连续尝试 3 次仍无法解决**时，才向用户描述问题并请求人工介入。
2. **当 Linter 报错（TypeScript/ESLint）时**：
   - 在修改文件后，如果 Agent 有诊断能力，应自动检查新增代码是否有语法错误。
   - 如有错误，立即修正，不要等到用户发现。

## 上下文与范围限制

1. 除非用户明确要求“分析整个项目”，否则 Agent 的操作范围应严格限定在当前 Phase（例如 Phase 1 只涉及 `src/entities` 和 `src/core`）。
2. 不要擅自修改 `README.md` 或 `AGENTS.md` 本身，除非用户要求更新文档。

## 交互反馈风格（针对视频录制优化）

1. **开始执行任务时**：用一句话简述目标，例如：“正在生成 16x16 阵列，将用到 InstancedMesh。”
2. **执行完成时**：用一句话汇总变更，例如：“已生成 256 个单元，帧率稳定 60fps。本次修改涉及 FormationUnit.js 和 main.js。”
3. **禁止长篇大论**：Agent 不需要输出“思考过程”的散文，直接给出结果和关键报错信息即可。

## 例外情况（必须回退到“建议模式”）

当遇到以下情况时，Agent 必须停止自动操作，转为给出建议方案：

- 用户提问：“你觉得这里该怎么设计？”
- 需要下载外部资产（如图片、字体）时。
- 涉及修改 `.vscode/launch.json` 或系统环境变量时。
